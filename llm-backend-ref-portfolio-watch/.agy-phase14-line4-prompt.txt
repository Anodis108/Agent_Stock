Implement ONLY Phase 14 line 4 in specs/implementation-plan.md:
`[ ] Structured output cho plan sơ đồ (nếu dùng LLM).`

Repo: d:/Hoc_Tap/YOURClass/Project/vn-stock-swarm/llm-backend-ref-portfolio-watch

Context (lines 1-3 done):
- diagram_agent returns static Mermaid via run_diagram_agent(symbol).
- UI renders diagram from data.diagram.mermaid.
- Pattern for LLM+structured: see event_classifier/nodes.py (LlmEventClassifier + Heuristic fallback + call_llm_structured).

Scope — ONLY line 4. Do NOT touch Phase 15 golden dataset.

Tasks:
1. `shared/schemas.py`: Add `DiagramPlanOutput` Pydantic schema:
   - title: str
   - nodes: list[str] (agent/step ids e.g. price_agent, news_agent)
   - edges: list[dict] with from/to keys OR use nested DiagramEdge model
   - mermaid: str (valid Mermaid body, no fences)
   - format: Literal["mermaid","json"] default "mermaid"
   - method `to_graph_json() -> dict` and/or helper to build DiagramAgentResult fields

2. `agents/diagram_agent/schemas.py`: re-export DiagramPlanOutput (like eval_agent/schemas.py).

3. `agents/diagram_agent/nodes.py`:
   - Keep `DiagramAgentResult` dataclass.
   - `HeuristicDiagramBrain`: current static scan-flow Mermaid (default, no LLM).
   - `LlmDiagramBrain`: prompt registry `diagram_plan` + call_llm_structured(..., DiagramPlanOutput); fallback to HeuristicDiagramBrain on error.
   - `run_diagram_agent(symbol, *, turn="", question="", brain=None, chat_fn=None, chat_parsed_fn=None)` uses brain pattern.

4. `prompts/diagram_plan/v1.yaml` + `production.txt` (follow other prompts in prompts/).

5. `graph/chat.py`: pass question + optional diagram_brain from deps to run_diagram_agent; include graph_json/plan in step output if available.

6. `graph/steps.py`: include plan/graph_json in diagram_agent step output when present.

7. `backend/ai_client.py`: if diagram_result has graph_json/plan fields, expose in `diagram` payload (extend existing diagram object).

8. `tests/test_phase14.py`: add `test_phase14_diagram_structured_llm_plan`:
   - Mock chat_parsed_fn returning DiagramPlanOutput with nodes/edges/mermaid
   - run_diagram_agent or run_chat_graph with LlmDiagramBrain injected
   - Assert diagram_result.mermaid valid + graph_json populated

9. Optional: 1-2 schema tests in test_structured_output.py for DiagramPlanOutput parse.

10. Mark line 4 `[x]` in implementation-plan.md; update change-log.md.

Constraints:
- Default production path can stay HeuristicDiagramBrain unless env enables LLM (match project conftest pattern — tests use heuristic/mocks).
- Minimal diff; match existing agent brain patterns.
- Run: `python -m pytest tests/test_phase14.py tests/test_structured_output.py -q --tb=line`

Output: files changed, pytest result.
