CONTINUE Phase 10 NOW. Do not wait for background tests.

Previous turn exited while "waiting for backend tests". Ignore that. Finish the file edits.

## Must do (Phase 10 only)
1. Restructure `src/portfolio_watch/frontend/` into Claude-like layout:
   - Left column: conversation (#chat-messages scrollable) + composer (#chat-form) fixed/at bottom of that column.
   - Right or secondary: placeholder for Phase 11 graph OR keep timeline/watchlist/approvals accessible (tabs / side / below) — do NOT delete them; do NOT build live graph.
2. Reuse existing chat API in `app.js` (POST chat). Surface network/timeout errors clearly in the thread or banner via `formatApiError`.
3. Desktop-first ~1280px; vanilla JS/CSS only.
4. Mark Phase 10 checklist `[x]` in `specs/implementation-plan.md`.
5. Append `specs/change-log.md` with layout decision, files touched, manual Docker demo steps:
   `docker compose up -d --build` → open http://127.0.0.1:8000/ → send 1 question → see answer or clear error.

## Verify cheaply
- Grep/layout in index.html + style.css
- Optional: `curl -s http://127.0.0.1:8000/ | head` if compose already up
- Do NOT block on full pytest suite

Edit the files yourself. Reply with files touched + demo steps when done.
