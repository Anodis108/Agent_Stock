You are implementing in d:/Hoc_Tap/YOURClass/Project/vn-stock-swarm/llm-backend-ref-portfolio-watch.

Task: Phase 15 line 5 ONLY — Gate injection 100%; regression vs v3_baseline + tolerance.

Context (already exists):
- src/portfolio_watch/eval/run.py has check_injection_gate, check_regression, eval_gates_passed, BASELINE_PATH defaults to v3_baseline.json
- SLICE_ORDER has 5 slices including diagram
- golden_v3.yaml: 33 cases; v3_baseline.json: 30/30 + diagram placeholder 0/0
- regression.py still uses golden_dataset.yaml + baseline_debug.json (OUTDATED)

Requirements (minimal diff):
1. Default golden dataset: when specs/eval/golden_v3.yaml exists, use it as default for --run (not golden_dataset.yaml). Keep --dataset override.
2. Add check_regression_by_slice(report, baseline, tolerance) in run.py:
   - For each slice in SLICE_ORDER where current report has total>0 AND baseline by_slice has numeric rate
   - Fail if (baseline_rate - current_rate) > tolerance
   - Skip baseline entries with rate null or total 0
   - Return dataclass with passed, failures list (slice, current, baseline, drop)
3. Wire gates in main():
   - After check_regression (overall), run and print check_regression_by_slice
   - eval_gates_passed must require: report.failed==0, overall regression OK, by_slice regression OK, injection 100%
4. Update src/portfolio_watch/eval/regression.py:
   - GOLDEN = golden_v3.yaml (fallback golden_dataset.yaml if missing)
   - BASELINE = v3_baseline.json (fallback baseline_debug.json)
   - run_one_case: pass --dataset specs/eval/golden_v3.yaml when using v3 golden
   - write_report slice order includes diagram
   - Exit 1 if regression OR injection gate fails
5. Tests in tests/test_eval.py:
   - test_regression_by_slice_catches_comparison_drop (mock report vs v3 baseline fixture)
   - test_regression_by_slice_skips_diagram_zero_baseline
   - test_eval_gates_passed_fails_on_injection_or_by_slice_regression
6. Extend _self_check with by_slice regression fixture (stub report vs temp baseline)
7. Do NOT re-run full 33-case eval to rewrite v3_baseline.json rates
8. Run: python -m pytest tests/test_eval.py tests/test_golden_v3.py -q && python -m src.portfolio_watch.eval.run --self-check

Output: files changed, test results, brief gate behavior summary.
