You are implementing in d:/Hoc_Tap/YOURClass/Project/vn-stock-swarm/llm-backend-ref-portfolio-watch.

Task: Phase 16 line 2 ONLY — `.env.example`: memory, Langfuse, freshness, Qdrant (optional).

Context:
- `.env.example` already has partial blocks from Phase 7-8 but checklist line 2 is still unchecked
- `src/portfolio_watch/shared/settings.py` defines: memory_short_term_window, memory_short_term_ttl_minutes (freshness), qdrant_*, embedding_*, monitoring_enabled, langfuse_*
- docker-compose.yml: optional qdrant profile; LANGFUSE_HOST override for Docker
- Existing tests: test_short_term_memory.test_env_example_has_memory_vars, test_long_term_memory.test_env_example_has_qdrant_vars, test_docker.test_env_example_has_langfuse_and_compose_vars

Requirements (minimal diff):
1. Reorganize `.env.example` — add clear V3 section header:
   `# ─── V3 — Memory, freshness, Langfuse, Qdrant (optional) ───`
   Group and document:
   - Memory short-term: MEMORY_SHORT_TERM_WINDOW, MEMORY_SHORT_TERM_TTL_MINUTES (explicit note: TTL = freshness minutes)
   - Long-term / Qdrant (optional): QDRANT_URL, QDRANT_API_KEY, QDRANT_COLLECTION, EMBEDDING_MODEL, EMBEDDING_DIM
     - Document: empty QDRANT_URL = in-memory fallback; `docker compose --profile qdrant up`
   - Langfuse / monitoring (optional): MONITORING_ENABLED, LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_HOST
     - Merge duplicate Langfuse comment blocks into ONE concise block (remove redundant "V2 Phase 14+" duplicate header if possible)
   - No real secrets; keep placeholders only
2. Ensure env var NAMES match settings.py fields (UPPER_SNAKE in .env → snake in Settings)
3. Add `tests/test_env_example_phase16.py` with `test_phase16_env_example_v3_memory_langfuse_freshness_qdrant`:
   - Assert all keys above present in .env.example
   - Assert comments mention freshness/TTL and Qdrant optional
   - Assert no `sk-proj-` real-looking committed secrets beyond placeholder comment
4. Do NOT change README, mvp-status-report, frontend (Phase 16 lines 3-5)
5. Run: python -m pytest tests/test_env_example_phase16.py tests/test_docker.py tests/test_short_term_memory.py tests/test_long_term_memory.py -q -k "env_example or phase16"

Output: summary of .env.example structure, test results.
