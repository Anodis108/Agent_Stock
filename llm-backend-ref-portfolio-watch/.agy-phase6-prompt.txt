You are implementing in the repo cwd (Portfolio Watch V3).

## Task
Implement ONLY Phase 6 from specs/implementation-plan.md — Structured output.

Checklist (do all):
- [ ] Schema Pydantic (hoặc tương đương) cho: rewrite, supervisor, classifier, eval, synthesis draft, memory extract.
- [ ] LLM path dùng structured output / parse schema — không dựa free-text thuần.
- [ ] Schema lỗi → retry/guard; **không** crash graph.
- [ ] Pytest tối thiểu 1 path rewrite + 1 path supervisor.

Done when: test structured pass.

## Context already done
- Phase 5: short prompts via `prompts/*/production.txt` + `infra/llm/prompt_registry.py`.
- `infra/llm/completion.py` already has `chat_parsed(messages, schema)` using OpenAI `chat.completions.parse`.
- LLM rewrite/route live in `agents/supervisor_agent/nodes.py` (`LlmRewriteBrain`, `LlmSupervisorBrain`) — currently `chat()` + `json.loads` / `_parse_json_obj`.
- Other LLM JSON paths: event_classifier, eval_agent, synthesis_agent, news_agent (ReAct) — prefer Pydantic parse/validate.
- Stub files `agents/*/schemas.py` mostly say "see nodes.py dataclasses" — fill with real Pydantic models (or a shared schemas package if cleaner).
- Memory extract may not exist as a node yet (Phase 7–8): still add a Pydantic schema for memory extract; wire only if a call site already exists, otherwise define schema + a small parse helper ready for later.
- Outer `rewrite_question` / `route_question` already catch Exception and fallback — keep that; add inner schema-fail retry/guard so graph does not crash.
- Do NOT start Phase 7+ (memory short-term, Langfuse, UI).

## Constraints
- Minimal diff; match existing style.
- Prefer reusing `chat_parsed` (or a thin wrapper that retries on ValidationError / parse miss, then falls back to safe defaults / heuristic) instead of new LLM stack.
- Tests must not require live LLM: mock `chat_parsed` / inject fake brain responses that go through schema validation, OR unit-test model_validate / parse helpers for rewrite + supervisor.
- Mark Phase 6 checklist `[x]` in specs/implementation-plan.md.
- Append specs/change-log.md with what schemas added, how retry/guard works, pytest commands + results.

## Verify
```bash
python -m pytest tests/ -q -k "structured or rewrite or supervisor" --maxfail=5
# or the new test file you add, e.g. tests/test_structured_output.py
python -m src.portfolio_watch.eval.run --self-check
```

## Output
What changed, files touched, exact manual test steps, pytest results.
Do the file edits yourself — do not only describe the change.
