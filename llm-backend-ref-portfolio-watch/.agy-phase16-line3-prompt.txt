You are implementing in d:/Hoc_Tap/YOURClass/Project/vn-stock-swarm/llm-backend-ref-portfolio-watch.

Task: Phase 16 line 3 ONLY — README chỉ Docker product + eval trong container.

Context:
- product-spec AC9 / out-of-scope: không dùng nhiều process uvicorn local làm đường chính; README chỉ Docker + eval in container
- Current README.md still has: Install (local), Chạy local (uvicorn), frontend :5173, local pytest, local curl smoke
- docker-compose.yml: single service `app`, eval: `docker compose run --rm app python -m src.portfolio_watch.eval.run ...`
- regression: `docker compose run --rm app python -m src.portfolio_watch.eval.regression`

Requirements (minimal diff):
1. Rewrite README.md to be Docker-first ONLY for running the product:
   - Prerequisites: Docker Desktop, copy .env.example → .env, OPENAI_API_KEYS if needed
   - Quick start: docker compose up --build, open http://localhost:8000
   - One URL table (app :8000; optional Langfuse/Qdrant notes brief)
   - Eval section with container commands ONLY:
     - self-check: docker compose run --rm app python -m src.portfolio_watch.eval.run --self-check
     - 1 case: docker compose run --rm app python -m src.portfolio_watch.eval.run --run --case-id lookup_01 --skip-judge
     - full regression: docker compose run --rm app python -m src.portfolio_watch.eval.regression
   - Troubleshooting (Docker-focused)
   - Specs links table (keep)
   - REMOVE or do NOT include as run instructions: local venv install, uvicorn local, http.server :5173, 3-process V2, local pytest as primary dev path
   - OK: one line "Contributors chạy pytest local — xem AGENTS.md" if needed, not a full local run guide
2. Add tests/test_readme_phase16.py:
   - test_readme_docker_product_only(): assert README contains docker compose up --build, eval run in container (docker compose run --rm app)
   - assert README does NOT contain primary local run sections like "## Chạy local" or "uvicorn src.portfolio_watch.backend.main" as main instructions
   - assert eval commands use `docker compose run --rm app`
3. Do NOT implement line 4 (status report) or line 5 (demo walkthrough) yet
4. Do NOT change frontend, .env.example, mvp-status-report
5. Run: python -m pytest tests/test_readme_phase16.py -q

Output: README structure summary, test results.
