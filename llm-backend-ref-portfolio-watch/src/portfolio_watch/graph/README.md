# graph/

LangGraph chat + scan — orchestration thật (Phase 13). Hiện re-export từ
`domain/graph/workflow.py` cho visualization và test.

**Legacy:** `domain/graph/` — di chuyển file vào đây ở Phase 13.
