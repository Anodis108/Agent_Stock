You are implementing in the repo cwd (Portfolio Watch V3).

## Task — ONE checklist item only
Implement ONLY this single unchecked line from specs/implementation-plan.md Phase 13:

- [ ] Quét + approve/reject hoạt động từ UI mới.

Do NOT implement the other Phase 13 line (no separate AI service).
Do NOT start Phase 14+.

## Acceptance (product-spec flow C)
- From `src/portfolio_watch/frontend/app.js` UI (Watchlist tab):
  - **Quét** via scan form or row button → `POST /scan` → timeline + live graph update; refresh approvals + market.
  - **Approve/Reject** on Approvals tab → `POST /approvals/{id}/approve|reject` → list refreshes; market status updates if pending cleared.
- Show clear error via existing `formatApiError` / `setBoot` on failure (no silent fail).
- Backend routes already on `backend/main.py` — wire/fix frontend only if gaps exist.

## Deliverables
1. Minimal frontend glue in `app.js` (and `index.html` only if needed).
2. Pytest:
   - `test_phase13_scan_approve_ui_wiring` in `tests/test_frontend.py` — assert JS has doScan, doApprove, doReject, POST `/scan`, `/approvals/`, and reload calls after actions.
   - `test_phase13_scan_and_approve_api` in `tests/test_backend.py` — TestClient: add pending approval, approve OK; POST /scan returns 200 with steps (mock AI if needed via existing fixtures or skip heavy LLM — prefer backend-only path with store).
3. Mark ONLY this line `[x]` in specs/implementation-plan.md.
4. Append specs/change-log.md for this line + manual test steps.

## Constraints
- Minimal diff; cheap pytest; no docker compose idle.

## Verify
```bash
python -m pytest tests/test_frontend.py tests/test_backend.py -q --tb=line -k "phase13_scan or phase13_single"
```

Do the file edits yourself.
