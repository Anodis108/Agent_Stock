You are implementing in the repo cwd (Portfolio Watch V3).

## Task
Implement ONLY Phase 3 from specs/implementation-plan.md — Docker product 1 service.

Checklist (do all of these):
- [ ] docker-compose.yml: main service named `app` on port 8000
- [ ] Optional service `qdrant` (profile or clearly commented)
- [ ] Stop requiring 3 services `frontend` + `backend` + `ai`
- [ ] SQLite volume — record path in specs/change-log.md
- [ ] Healthcheck on compose service `app`

Done when: `docker compose up --build` → one primary product URL.

## Context (Phase 2 already done)
- Single FastAPI entry: `uvicorn src.portfolio_watch.backend.main:app`
- Serves API + static UI from `src/portfolio_watch/frontend/`
- LangGraph in-process via `backend/ai_client.py` (default AI_TRANSPORT=inprocess)
- Do NOT re-implement Phase 2.

## Constraints
- Minimal diff; match existing style.
- Do not add unrelated features (no Phase 4+).
- Update Dockerfile CMD/default to run `backend.main:app` on port 8000 if needed.
- Keep env_file `.env`; SQLITE_PATH under volume e.g. `/app/data/portfolio_watch.db`.
- Update README briefly for one-URL Docker (product path).
- Mark Phase 3 checklist items `[x]` in specs/implementation-plan.md.
- Append entry to specs/change-log.md (what / volume path / how to test).

## Verify
- Prefer: `docker compose up --build -d` then curl http://127.0.0.1:8000/health and GET /
- If full rebuild is too slow on this machine, still leave compose correct and note known issue in change-log.
- Run cheap tests if relevant (e.g. tests that assert compose layout).

## Output
What changed, files touched, exact manual test steps.
Do the file edits yourself — do not only describe the change.
