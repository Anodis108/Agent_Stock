You are implementing in the repo cwd (Portfolio Watch V3).

## Task — ONE checklist item only
Implement ONLY this single unchecked line from specs/implementation-plan.md Phase 14:

- [ ] Output Mermaid hoặc graph JSON.

Do NOT implement Phase 14 lines 3–4 (UI render, structured LLM plan).
Do NOT start Phase 15+.

## Acceptance (line 2 only)
1. `diagram_agent` produces **real** diagram output (not placeholder-only):
   - `DiagramAgentResult` fields: `mermaid: str`, `graph_json: dict | None`, `format: "mermaid"|"json"`.
   - For scan-flow requests: static/template Mermaid OK (reuse SCAN_NODES from `domain/graph/workflow.py` or hardcode scan pipeline nodes) — no LLM required for this line.
   - Optional symbol in title (e.g. FPT).
2. Chat answer includes Mermaid fenced block or embed `diagram` object in response payload (`answer` text with ```mermaid ... ``` is fine).
3. `graph/steps.py` diagram_agent step output includes `mermaid` snippet (for hover I/O).
4. `backend/ai_client.py` forward diagram fields if chat response exposes them (minimal).
5. Pytest `test_phase14_diagram_outputs_mermaid` in `tests/test_phase14.py`:
   - Question "Vẽ sơ đồ luồng scan FPT" → result has mermaid containing `graph TD` or `flowchart` and scan node names (price_agent, etc.).
6. Mark ONLY this line `[x]` in specs/implementation-plan.md.
7. Append specs/change-log.md for this line + manual test steps.

## Key files
- `src/portfolio_watch/agents/diagram_agent/nodes.py`
- `src/portfolio_watch/graph/chat.py` (_node_diagram_agent)
- `src/portfolio_watch/graph/steps.py`
- `tests/test_phase14.py`

## Constraints
- Minimal diff; no UI/frontend changes; cheap pytest; edit repo cwd NOT scratch.

## Verify
```bash
python -m pytest tests/test_phase14.py -q --tb=line
```

Do the file edits yourself.
