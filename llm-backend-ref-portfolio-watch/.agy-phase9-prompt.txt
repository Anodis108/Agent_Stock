You are implementing in the repo cwd (Portfolio Watch V3).

## Task
Implement ONLY Phase 9 from specs/implementation-plan.md — Langfuse: 1 request = 1 trace.

Checklist (do all):
- [ ] Đúng 1 root `trace_request` / chat đến END.
- [ ] Đúng 1 root / scan đến END.
- [ ] Mỗi graph node = span tên = node id; mọi step con có **input + output**.
- [ ] `MONITORING_ENABLED=false` → no-op, chat 200.
- [ ] Pytest mock hierarchy parent/child.

Done when: mock test pass; checklist thủ công ghi trong change-log (nếu có host).

## Context already present (build on it — do not rewrite from scratch)
- `infra/monitoring/tracing.py`: `trace_request`, `agent_span`, `agent_step`, `trace_step`; no-op when `MONITORING_ENABLED=false` or missing keys.
- Chat root: `application/answer_question.py` wraps `run_chat_graph` in `trace_request("chat", ...)`.
- Scan root: `application/scan_symbol.py` wraps `run_scan_graph` in `trace_request("scan", ...)`.
- Graph nodes already call `agent_span(turn, "<node_id>", input=...)` in `graph/chat.py` / `graph/scan.py`.
- Some agent internals use `agent_step(turn, agent_name, step_name)` — verify parent name matches the outer `agent_span` name so steps nest (e.g. rewrite uses span `rewrite_question` but step may look up `supervisor` — fix mismatches).
- Existing tests: `tests/test_tracing.py` (mostly no-op). Product-spec AC #3 / test-plan §2 require **mock hierarchy parent/child**.
- Settings / `.env.example` already have MONITORING_* / LANGFUSE_*.
- Do NOT start Phase 10+ (UI).

## Constraints
- Minimal diff; match existing style; keep monitoring best-effort (never crash chat/scan).
- Exactly one root per chat request and one root per scan request (no duplicate roots).
- Every graph node span must set **input and output** on the box when data exists (fill gaps where `box["output"]` is missing).
- Pytest: mock Langfuse client / observation objects (no live Langfuse host required) asserting:
  - 1 root for chat path; 1 root for scan path;
  - child agent spans under root;
  - parent/child nesting (step under agent or agent under root);
  - no-op when monitoring disabled (chat still 200).
- Mark Phase 9 checklist `[x]` in specs/implementation-plan.md.
- Append specs/change-log.md: what was fixed, mock test results, and a short manual checklist for when Langfuse host is available (even if not run here).

## Verify
```bash
python -m pytest tests/test_tracing.py tests/ -q -k "trace or langfuse or monitoring" --maxfail=8
python -m src.portfolio_watch.eval.run --self-check
# Optional: MONITORING_ENABLED=false curl/TestClient chat 200
```

## Output
What changed, files touched, exact manual test steps, pytest results.
Do the file edits yourself — do not only describe the change.
