You are implementing in the repo cwd (Portfolio Watch V3).

## Task — ONE checklist item only
Implement ONLY this single unchecked line from specs/implementation-plan.md Phase 13:

- [ ] Không gọi AI service tách (nếu đã gộp).

Do NOT start Phase 14+.

## Acceptance
- Product default: `AI_TRANSPORT=inprocess` (docker-compose + `.env.example`); LangGraph qua `backend/ai_client.py` in-process — browser/frontend KHÔNG gọi `:8001` hay `/v1/*`.
- Document/assert in code comments only if needed (minimal).
- Pytest:
  - `test_phase13_no_separate_ai_from_frontend` — `tests/test_frontend.py`: no `8001`, no `/v1/`, no `AI_BASE_URL` fetch in app.js.
  - `test_phase13_inprocess_default` — `tests/test_docker.py` or `tests/test_backend.py`: compose has `AI_TRANSPORT: "inprocess"`; default `_use_http()` false when env unset; chat works in-process (reuse existing `test_chat_inprocess` pattern or lightweight assert).
- Mark ONLY this line `[x]` in specs/implementation-plan.md.
- Append specs/change-log.md for this line + manual verify steps.

## Constraints
- Minimal diff; no new services; cheap pytest only.

## Verify
```bash
python -m pytest tests/test_frontend.py tests/test_backend.py tests/test_docker.py -q --tb=line -k "phase13_no_separate or phase13_inprocess or inprocess"
```

IMPORTANT: Edit files in the repo cwd directly (NOT scratch folder).

Do the file edits yourself.
