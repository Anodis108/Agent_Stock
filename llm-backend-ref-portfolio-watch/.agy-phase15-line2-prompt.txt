Implement ONLY Phase 15 line 2 in specs/implementation-plan.md:
`[ ] Slice: lookup, comparison, out_of_scope, injection, diagram (≥3 case).`

Repo: d:/Hoc_Tap/YOURClass/Project/vn-stock-swarm/llm-backend-ref-portfolio-watch

Context:
- `specs/eval/golden_v3.yaml` already has 30 cases: lookup 18, comparison 6, out_of_scope 3, injection 3 (Phase 15 line 1).
- Phase 14 diagram agent: "Vẽ sơ đồ luồng scan FPT" → answer with ```mermaid, diagram.mermaid with graph TD + price_agent.
- test-plan diagram slice gate: "Có Mermaid/JSON sơ đồ hợp lệ" — use must_include for diagram markers.

Scope — ONLY line 2 (add diagram slice cases). Do NOT:
- Wire eval runner to golden_v3 (line 4)
- Change regression/baseline (line 5)
- Modify eval/run.py GOLDEN_PATH yet
- Implement line 3 rule-based changes beyond adding must_* on new cases

Tasks:
1. Append ≥3 diagram cases to `specs/eval/golden_v3.yaml`:
   - ids: diagram_01, diagram_02, diagram_03 (optional diagram_04)
   - slice: { type: diagram, difficulty: easy|medium }
   - Questions in Vietnamese about drawing scan/workflow diagrams (FPT, VNM, HPG variants)
   - expected: describes valid Mermaid/graph output
   - must_include: tokens that heuristic diagram agent produces (e.g. "graph", "price_agent" or "mermaid")
   - must_not_include: ["nên mua", "nên bán"]
   - Match style of existing cases in file

2. Update `golden_v3.yaml` changelog to note diagram slice added.

3. Update `tests/test_golden_v3.py`:
   - Expect total cases >= 33
   - Assert diagram slice count >= 3
   - All 5 slice types present: lookup, comparison, out_of_scope, injection, diagram

4. Mark ONLY line 2 `[x]` in specs/implementation-plan.md

5. Update specs/change-log.md

Constraints:
- Keep existing 30 cases unchanged
- Minimal diff
- Run: `python -m pytest tests/test_golden_v3.py -q --tb=line`

Output: files changed, pytest result.
