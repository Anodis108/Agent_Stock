You are implementing in the repo cwd (Portfolio Watch V3).

## Task
Implement ONLY Phase 11 from specs/implementation-plan.md — Live graph + hover I/O.

Checklist (do all):
- [ ] Panel phải: node theo `steps[]` (hoặc event tương đương).
- [ ] Node sáng lần lượt khi chạy.
- [ ] Hover node → hiện input + output của bước đó.
- [ ] API/response đủ field I/O cho UI (nếu thiếu thì bổ sung contract).

Done when: 1 chat thấy node sáng + hover có I/O (document manual Docker steps in change-log).

## Context
- Phase 10 Claude layout is done: left chat, right `.secondary-column` with `.graph-placeholder-card` (REPLACE this with live graph — do not keep empty placeholder).
- Timeline tabs stay; graph is the live visualization of the same run.
- Frontend: `src/portfolio_watch/frontend/` (`index.html`, `app.js`, `style.css`).
- Today steps shape is `{id, name, status, detail?}` only. `backend/steps.py` `normalize_steps` STRIPS unknown keys — you MUST extend contract to pass `input` + `output` (objects or strings OK; JSON-serializable).
- Steps are built in `src/portfolio_watch/graph/steps.py` (`build_steps_from_chunks`) and possibly application helpers — populate `input`/`output` meaningfully per node (e.g. rewrite: in=question out=rewritten; price: in=symbol out=quote summary; etc.).
- Chat/scan are one-shot HTTP (no SSE). For “node sáng lần lượt”: after response arrives, client-side sequential highlight animation over `steps[]` is ACCEPTABLE (pending→running→done). Optimistic `showTimelineStart` can also briefly show running.
- Do NOT embed/reuse `docs/agent_graph.html|.mmd|.png` or `domain/graph/workflow.py` draw — those are static architecture docs, not the live panel.
- Do NOT start Phase 12+ (Market status, diagram agent, etc.).

## Constraints
- Minimal diff; vanilla JS/CSS; reuse existing chat API + `applyTimelineFromBackend` / `normalizeSteps` — extend them for input/output.
- Desktop-first; hover (or focus) shows a popover/panel with that step’s input + output.
- Keep watchlist / approvals / timeline tabs working.
- Mark Phase 11 checklist `[x]` in specs/implementation-plan.md.
- Append specs/change-log.md: I/O contract decision, files touched, manual demo steps (compose → chat → see nodes light → hover I/O).
- Prefer cheap verify: pytest on touched tests / frontend tests; optional curl chat; do NOT block forever on docker rebuild or full suite. If docker is slow, finish code + document demo steps and exit.

## Verify
```bash
python -m pytest tests/test_frontend.py tests/test_backend.py -q --tb=line  # or narrower if needed
# optional: curl POST /chat and confirm steps[].input / steps[].output present
```

## Output
What changed, files touched, exact manual demo steps.
Do the file edits yourself — do not only describe the change.
Do NOT idle waiting for background docker/tests — finish edits, mark checklist, write change-log, then reply.
