Implement ONLY Phase 15 line 1 in specs/implementation-plan.md:
`[ ] specs/eval/golden_v3.yaml: version, mỗi case có id + slice.`

Repo: d:/Hoc_Tap/YOURClass/Project/vn-stock-swarm/llm-backend-ref-portfolio-watch

Context:
- Existing v1 dataset: specs/eval/golden_dataset.yaml (30 cases: 18 lookup, 6 comparison, 3 out_of_scope, 3 injection).
- test-plan.md shows golden_v3 format: dataset, version: 3, changelog, cases with id + slice { type, difficulty }.
- v3_baseline.json notes diagram slice will be added in Phase 15 later lines.

Scope — ONLY line 1 (create versioned file + id/slice structure). Do NOT:
- Add diagram cases (Phase 15 line 2)
- Wire runner to golden_v3 (line 4)
- Change regression gate (line 5)
- Modify eval/run.py GOLDEN_PATH yet

Tasks:
1. Create `specs/eval/golden_v3.yaml`:
   - dataset: portfolio_watch (or portfolio_watch_chat_golden)
   - version: "3" or 3
   - changelog describing v3 carry-forward from v1
   - Migrate all 30 cases from golden_dataset.yaml preserving id, question, expected, must_include, must_not_include
   - Each case MUST have `id` and `slice` with at least `type` (lookup|comparison|out_of_scope|injection)
   - Add `difficulty: easy|medium|hard` per test-plan example where sensible
   - Do NOT add diagram slice cases yet

2. Add `tests/test_golden_v3.py`:
   - File exists and loads as YAML
   - version == 3 (or "3")
   - len(cases) == 30
   - Every case has unique id and slice.type
   - Slice types present: lookup, comparison, out_of_scope, injection (counts match v1: 18/6/3/3)

3. Mark ONLY line 1 `[x]` in specs/implementation-plan.md

4. Update specs/change-log.md with entry + manual verify steps

Constraints:
- Do not delete golden_dataset.yaml (runner still uses it until line 4)
- Minimal diff elsewhere
- Run: `python -m pytest tests/test_golden_v3.py -q --tb=line`

Output: files changed, pytest result.
